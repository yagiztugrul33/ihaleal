const fs=require("fs");const p="src/pages/intelligence/ParcelIntelligencePage.tsx";const mc="</motion.div>";const dc="</motion.div>".replace("motion.","");let lines=fs.readFileSync(p,"utf8").split("\n");for(let i=0;i<lines.length;i++){if(lines[i].trimStart().startsWith("<div>")&&lines[i].trimEnd().endsWith(mc))lines[i]=lines[i].slice(0,lines[i].lastIndexOf(mc))+dc;}for(let i=0;i<lines.length;i++){if(lines[i].includes("mb-6 flex gap-2 text-xs")){for(let j=i+1;j<lines.length;j++){if(lines[j].trim()===mc){lines[j]=lines[j].replace(mc,dc);break;}if(lines[j].includes("rounded-lg border border-cyan-500"))break;}}}for(let i=0;i<lines.length;i++){if(lines[i].includes("flex gap-2")&&lines[i].trimStart().startsWith("<motion.div")){for(let j=i+1;j<i+8;j++){if(lines[j].trim()===mc){lines[j]=lines[j].replace(mc,dc);break;}}}}fs.writeFileSync(p,lines.join("\n"),"utf8");console.log("ok");