================================================================================
GENEL — BU DOSYA İNSAN + YAPAY ZEKA İÇİN ANAŞART
================================================================================
- Bu dosyayı (CLOUD_KOMUTLARI.txt) silmeden / yeniden adlandırmadan düzenleyin.
- Cloud veya Cursor: önce docs/OZELLIK_ENVANTERI_SAHIBINDEN_ESLENIK.txt dosyasını okuyun.
- Zorunlu çıktı klasörü: CLOUD_CIKTI/ — rapor buraya yazılır (aşağıdaki komutlarda tekrarlanır).
- Hedef ürün vizyonu: kullanıcı hem İHALEYE (alıcı) girebilsin hem İLAN VEREBİLSİN (satıcı);
  sahibinden.com seviyesinde vitrin + ihaleal farkı (komisyon modeli, şeffaflık, analiz).


================================================================================
KOMUT A — CLOUD AGENT: TAMAMLAMA + SEO + İHALEAL ENDEKSI + SAHİBİNDEN PARİTESİ + RAPOR DOSYASI
================================================================================
Aşağıdaki metni TEK parça olarak cloud agent'a yapıştır.

---
SEN: Kıdemli ürün mühendisi + frontend mühendisi + SEO uzmanı + QA. Proje kökü bu repo.

HEDEF
1) Siteyi bozmadan `npm run typecheck` ve `npm run build` yeşil olsun.
2) SEO: `src/lib/seo.ts` + `SeoSync`, `index.html` schema, `public/sitemap.xml` / `robots.txt`,
   HashRouter uyumu, `ShareButton` tam URL. Eksik route meta varsa ekle.
3) İhaleal Endeksi: canlı API uydurma; `src/pages/DataStrategy.tsx` ve `docs/` ile uyumlu
   TEKNİK ve HUKUKİ sınırları açık yaz. `.env.example` ile uyumlu env isimleri kullan.
   Gerçek anahtar / ücretli servis ekleme.
4) Sahibinden paritesi: docs/OZELLIK_ENVANTERI_SAHIBINDEN_ESLENIK.txt içindeki maddeleri
   güncelle (Var/Kısmi/Yok). Alıcı + satıcı akışlarında kodda düzeltilebilir küçük UX
   kopukluklarını gider (büyük refactor yok).
5) Halüsinasyon avı: "çalışıyor" denilen ama olmayan entegrasyon iddiasını metinlerden temizle.

ZORUNLU ÇIKTI (DOSYA — CLOUD'A ÖZEL)
Çalışma sonunda şu dosyayı OLUŞTUR veya güncelle (Markdown):

  CLOUD_CIKTI/AGENT_RAPORU.md

İçerik bölümleri (şablonu doldur):
  # Cloud Agent Raporu
  ## 1. Özet (max 8 cümle)
  ## 2. Ortam: Node/npm, typecheck/build/audit sonucu
  ## 3. Sahibinden-benzeri özellik matrisi (Alıcı | Satıcı | SEO | İhaleal Endeksi) — tablo: Özellik | Durum | Dosya
  ## 4. SEO denetimi: route meta, canonical, OG, SearchAction, sitemap notları
  ## 5. Lisanslı veri / İhaleal Endeksi entegrasyonu: yapılabilir adımlar / blokajlar (API sözleşmesi, KVKK)
  ## 6. Yapılan commit'e uygun kod değişiklikleri listesi
  ## 7. KALAN İŞLER (backend, ödeme, e-Devlet, SSR, gerçek e-posta…)
  ## 8. Halüsinasyon / yanlış ifade tespiti (bulunduysa alıntı + düzeltme)

KISITLAR
- CLOUD_KOMUTLARI.txt dosyasını silme veya içeriğini komple değiştirme.
- Ücretli API anahtarı veya gizli bilgi repo içine yazma.
- HashRouter'ı BrowserRouter'a çevirme (hosting stratejisi kullanıcı onayı olmadan değişmesin).

ÇIKTI (sohbet mesajında)
- Kısa özet + AGENT_RAPORU.md yolunu tekrarla + KALAN İŞLER başlığını özetle.
---


================================================================================
KOMUT B — CLOUD AGENT: KÖKEN + HALÜSİNASYON + AYNI RAPOR DOSYASINA EK BÖLÜM
================================================================================
Ayrı bir oturumda veya Komut A'dan sonra yapıştır.

---
SEN: Provenance ve kalite denetçisisin. Aynı repo kökü.

GÖREV
1) Git varsa: `git log --oneline -25`, `git status`. Yoksa bunu raporda belirt.
2) Kaynakta case-insensitive ara: kimi, moonshot, generated by, TODO: remove, placeholder api.
3) package.json name/author/repository ile klasör adı uyumu.
4) docs/OZELLIK_ENVANTERI_SAHIBINDEN_ESLENIK.txt ile kod gerçekliği çelişiyor mu kontrol et.

ZORUNLU ÇIKTI
CLOUD_CIKTI/AGENT_RAPORU.md dosyasına yeni bölüm ekle (dosya yoksa oluştur):

  ## 9. Kök iz ve dosya sahipliği
  - Şüpheli/bilinçli yorum satırları
  - Üçüncü parti şablon izleri
  ## 10. Özellik iddiası vs kod doğrulaması (çelişki tablosu)

KISIT
Kişisel veri veya token bulursan repoya yazma; raporda "kaldırılmalı" de.

Sohbet çıktısı: bulgular özeti + AGENT_RAPORU.md güncellendi teyidi.
---


================================================================================
CURSOR (YEREL) İÇİN — AYNI KOMUTLARI BANA YAPIŞTIRDIĞINDA
================================================================================
Sen yukarıdaki KOMUT A veya B metnini uygula; fark olarak:
- CLOUD_CIKTI/AGENT_RAPORU.md dosyasını ben oluşturup doldurayım.
- Değişiklikleri doğrudan workspace'e yazayım; build çalıştırayım.

Rekabet cümlesi kullanıcıya ait: aynı talimat iki ortamda da ölçülebilir; ölçüt
typecheck + build + AGENT_RAPORU.md tamlığıdır.
