# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
!.env.production.example
.env.local
.env.*.local
.env.local.bak
.env.local.bak*
.env.*.bak
.env.*.bak*
*.env.bak
*.env.bak*

# Environment files (explicit)

tmp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Örnek / üçüncü taraf sözleşme taramaları (isteğe bağlı; repoya almak istemezseniz açın)
# docs/hukuk/kaynak/*.pdf

# Test / coverage
coverage
*.lcov
test-results
playwright-report

# OS
Thumbs.db

# Supabase local (opsiyonel)
.branches
.temp

# Anahtar dosyası yanlışlıkla repoya sürüklenirse
supabase-keys.txt

# Yerel yedek çıktısı (script masaüstüne yazar; repoda oluşursa)
ihaleal-backups/
.vercel

# AI/IDE lokal state (Claude Code session/plan dosyaları)
# NOT: .claude/skills/ versiyonlanır (paylaşılan skill'ler), gerisi lokal kalır.
.claude/*
!.claude/skills/

# Web UI QA screenshot artefaktları (Playwright/manuel test çıktıları)
screenshots/

# Capacitor Android — build çıktıları + IDE state
# (Kaynak commit'li, build artefaktları YOK — Master Android Studio'da generate eder)
android/.gradle/
android/build/
android/app/build/
android/app/release/
android/captures/
android/.idea/
android/local.properties
android/*.iml
android/app/*.iml
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
android/app/src/main/assets/capacitor.plugins.json
*.keystore
*.jks
key.properties

# Capacitor iOS — build çıktıları (macOS Xcode'da generate edilecek)
ios/App/Pods/
ios/App/build/
ios/App/App.xcworkspace/xcuserdata/
ios/.cocoapods/
ios/App/output/
