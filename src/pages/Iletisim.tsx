import { Contact } from "@/sections/Contact";

/** Kurumsal URL için iletişim — ana sayfadaki bölümün aynısı. */
export default function Iletisim() {
  return (
    <div className="min-h-screen pt-16">
      <Contact />
    </div>
  );
}
