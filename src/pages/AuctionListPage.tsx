import { Auctions } from "@/sections/Auctions";

/** Tam liste: 24 demo ihale, filtre ve sıralama `Auctions` bileşeninde. */
export default function AuctionListPage() {
  return <Auctions hideIntro={false} layout="page" />;
}
