/** Maden / mining rights variant details. */

export interface MadenDetay {
  mineralType?: string;
  licenseNo?: string;
  reserveTon?: number;
  licenseExpiry?: string;
  explorationStage?: string;
  royaltyPercent?: number;
  environmentalImpactApproved?: boolean;
}
